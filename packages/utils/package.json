{
  "name": "@bidconnect/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "date-fns": "^3.3.1",
    "date-fns-tz": "^2.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@bidconnect/typescript-config": "workspace:*",
    "@bidconnect/eslint-config": "workspace:*",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}